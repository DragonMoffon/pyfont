"""
Required Tables:
    cmap (character to glyph mapping) - Created
    head (font header) - Created, Parsable, Parsable
    hhea (horizontal header) - Created, Parsable
    hmtx (horizontal metrics) - Created, Parsable
    maxp (maximum profile) - Created, Parsable
    name (naming) - Created, Parsable (Missing Mac encodings)
    OS/2 ( OS/2 and windows specific metrics) - Created, Parsable
    post (PostScript) - Created, Parsable

Shared Tables:
    cvt (control value)
    fpgm (font program)
    glyf (glyph outline) - Created
    loca (index to location) - Created
    gasp (grid-fitting and scan-conversion procedure)

    sbix (extended bitmaps)
    EBSC (embedded bitmap scaling control)

    avar (axis variation)
    cvar (CVT variation)
    fvar (font variation)
    gvar (glyph variation)

    kern (kerning)
    meta (metadata)
    hdmx (horizontal device metrics)
    vmtx (vertical metrics)
    vhea (vertical header)

True Type Tables:
    acnt (accent attachment)
    ankr (anchor point)
    bdat (bitmap data)
    bhed (bitmap font header)
    bloc (bitmap location)
    bsln (baseline)
    fdsc (font descriptor)
    feat (layout feature)
    fmtx (font metrics)
    fond (font family compatibility)
    just (justification)
    kerx (extended kerning)
    lcar (ligature caret)
    ltag (language tag)
    mort (metamorphosis) table (deprecated)
    morx (extended metamorphosis)
    opbd (optical bounds)
    prop (properties)
    trak (tracking)
    xref (cross-reference)
    Zapf (glyph reference)

Open Type Tables:
    SVG (scalar vector graphic glyphs)

    CFF (Compact Font Format 1.0)
    CFF2 (Compact Font Format 2.0)
    VORG (Vertical Origin)
    EBDT (Embedded bitmap data)
    EBLC (Embedded bitmap location data)
    CBDT (Color bitmap data)
    CBLC (Color bitmap location data)

    BASE (Baseline data)
    GDEF (Glyph definition data)
    GPOS (Glyph positioning data)
    GSUB (Glyph substitution data)
    JSTF (Justification data)
    MATH (Math layout data)

    HVAR (Horizontal metrics variations)
    MVAR (Metrics variations)
    STAT (Style attributes)
    VVAR (Vertical metrics variations)

    COLR (Color table)
    CPAL (Color palette table)

    DSIG (Digital Signature)
    LTSH (Linear threshold map)
    MERG (Merge)
    STAT (Style attributes)
    PCLT (PCL 5 data)
    VDMX (Vertical device metrics)
"""
